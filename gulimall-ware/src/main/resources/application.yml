#配置数据库驱动
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://192.168.12.132:3306/gulimall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  application:
    name: gulimall-ware
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss


#mysql映射文件,自增组件
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 12000
logging:
  level:
    com.xie: debug